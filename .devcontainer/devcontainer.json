{
  "name": "Codespace Tailscale",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y supervisor",
  "postStartCommand": "sudo supervisord -c .devcontainer/supervisord.conf"
}
